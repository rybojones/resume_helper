"""Main orchestrator — no CLI concerns."""


def build_resume(
    resume_path: str | None,
    job_input: str,
    projects_path: str | None,
    role_tag: str | None,
    provider: str,
    output_path: str | None,
) -> None:
    raise NotImplementedError("resume_builder not yet implemented")
